{
  "name": "@jsonic/csv",
  "version": "0.9.0",
  "description": "This plugin allows the [Jsonic](https://jsonic.senecajs.org) JSON parser to support csv syntax.",
  "main": "dist/csv.js",
  "type": "commonjs",
  "browser": "csv.min.js",
  "types": "dist/csv.d.ts",
  "homepage": "https://github.com/jsonicjs/csv",
  "keywords": [
    "pattern",
    "matcher",
    "object",
    "property",
    "json"
  ],
  "author": "Richard Rodger (http://richardrodger.com)",
  "repository": {
    "type": "git",
    "url": "git://github.com/jsonicjs/csv.git"
  },
  "scripts": {
    "test": "node --test dist-test/csv.test.js",
    "test-some": "node --test --test-name-pattern",
    "test-watch": "node --test --watch dist-test/csv.test.js",
    "watch": "tsc --build src test -w",
    "doc": "jsonic-doc",
    "build": "tsc --build src test",
    "prettier": "prettier --write --no-semi --single-quote src/*.ts test/*.ts",
    "clean": "rm -rf node_modules dist dist-test yarn.lock package-lock.json",
    "reset": "npm run clean && npm i && npm run build && npm test",
    "repo-tag": "REPO_VERSION=`node -e \"console.log(require('./package').version)\"` && echo TAG: v$REPO_VERSION && git commit -a -m v$REPO_VERSION && git push && git tag v$REPO_VERSION && git push --tags;",
    "repo-publish": "npm run clean && npm i && npm run repo-publish-quick",
    "repo-publish-quick": "npm run prettier && npm run build && npm run test && npm run doc && npm run repo-tag && npm publish --access public --registry https://registry.npmjs.org "
  },
  "license": "MIT",
  "files": [
    "dist",
    "LICENSE"
  ],
  "devDependencies": {
    "@jsonic/doc": "^0.0.9",
    "@types/node": "^25.3.3",
    "csv-spectrum": "^2.0.0",
    "esbuild": "^0.27.3",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3"
  },
  "peerDependencies": {
    "jsonic": ">=2"
  }
}
